{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Recipe creation</h1>

    {{ form(form) }}
    <hr>
        <ul class="record_actions">
    <li>
        <a href="{{ path('recipe') }}">
            Back to the list
        </a>
    </li>
</ul>
        <hr>
{#        logged user: {{ user }}#}
{% endblock %}
