{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SecondaryIngredient creation</h1>

    <div class="container">
        <div class="row">
            <div class="col-md-offset-4">
                <div class="col-md-4">
                    {{ form(form) }}

                    <ul class="record_actions">
                        <li>
                            <a href="{{ path('secondaryingredient') }}">
                                Back to the list
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
